#ifndef H_PATH_NODE
#define H_PATH_NODE

#include "platform.h"

class PathNode
{
public:
    PathNode(Platform* platform)
private:
    Platform* platform; //The platform that this node is on
}
#endif
